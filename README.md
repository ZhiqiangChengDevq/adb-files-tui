# adb-files-tui
a tui file manager by adb tools

## Build

FTXUI is required and can be installed with Homebrew:

```sh
brew install ftxui
```

```sh
cmake -S . -B build
cmake --build build
```

## Run

```sh
./build/adb-files-tui
```
